exch_first = float(72 / 100)
exch_follow = float(10.56 / 100)
exch_compr = float(70 / 100)
exch_incr = float(50.23 / 100)

oracle_first = float(100 / 100)
oracle_follow = float(36.56 / 100)
oracle_compr = float(25 / 100)
oracle_incr = float(100 / 100)

sql_first = float(100 / 100)
sql_follow = float(36.56 / 100)
sql_compr = float(25 / 100)
sql_incr = float(100 / 100)

shares_first = float(72 / 100)
shares_follow = float(9.72 / 100)
shares_compr = float(70 / 100)
shares_incr = float(51.88 / 100)

engineering_first = float(30 / 100)
engineering_follow = float(17.34 / 100)
engineering_compr = float(50 / 100)
engineering_incr = float(50 / 100)

uncompressible_first = float(70 / 100)
uncompressible_follow = float(5.82 / 100)
uncompressible_compr = float(100 / 100)
uncompressible_incr = float(93.88 / 100)

vm_first = float(70 / 100)
vm_follow = float(5.82 / 100)
vm_compr = float(65 / 100)
vm_incr = float(93.88 / 100)
